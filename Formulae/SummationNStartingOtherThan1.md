### Sum of Integers from `b` to `a` (where `a > b`)

If the range is from `a` to `b` and `a > b`, the sum of all integers from `b` to `a` (inclusive) can be calculated using the formula:

```math
\text{sumN} = \frac{a(a + 1)}{2} - \frac{(b - 1)b}{2}
```
